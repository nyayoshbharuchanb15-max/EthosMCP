{
  "_comment": [
    "EthosMCP — Claude Desktop / Cursor connection configuration",
    "",
    "Copy the 'ethosmcp' block into your Claude Desktop config file:",
    "  macOS : ~/Library/Application Support/Claude/claude_desktop_config.json",
    "  Windows: %APPDATA%\\Claude\\claude_desktop_config.json",
    "",
    "Then restart Claude Desktop. EthosMCP will appear in the MCP server list.",
    "",
    "The server launches via stdio, so no port needs to be open.",
    "Replace '/absolute/path/to/EthosMCP' with your actual clone path."
  ],
  "mcpServers": {
    "ethosmcp": {
      "command": "python",
      "args": [
        "-m",
        "src.cli",
        "serve",
        "--transport",
        "stdio"
      ],
      "cwd": "/absolute/path/to/EthosMCP",
      "env": {
        "CRYPTO_KEY": "replace_with_strong_random_key",
        "ETHOSMCP_DATA_DIR": "data"
      }
    }
  }
}
