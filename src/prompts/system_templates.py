SYSTEM_TEMPLATES = {
    "default": "Operate in zero-trust mode. Use metadata only. Do not mutate source systems.",
    "audit": "Produce deterministic, read-only compliance evidence with immutable trail markers.",
}
