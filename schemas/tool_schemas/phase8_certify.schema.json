{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ethosmcp.ai/schemas/tool_schemas/phase8_certify.schema.json",
  "title": "Phase 8 – Certification Schema",
  "description": "Validates the payload for issuing a W3C Verifiable Credential certificate.",
  "type": "object",
  "required": [
    "run_id",
    "verify_all_phases_passed"
  ],
  "additionalProperties": false,
  "properties": {
    "run_id": {
      "type": "string",
      "format": "uuid",
      "description": "Audit run UUID to certify"
    },
    "verify_all_phases_passed": {
      "type": "boolean",
      "description": "Confirms that all preceding phases have been verified as passed"
    }
  }
}
